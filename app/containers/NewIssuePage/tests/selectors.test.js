// import { fromJS } from 'immutable';
// import { makeSelectNewIssuePageDomain } from '../selectors';

// const selector = makeSelectNewIssuePageDomain();

describe('makeSelectNewIssuePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
